meta {
  name: Add Account
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/accounts
  body: json
}

body:json {
  {
    "provider": "gdrive",
    "headless": true
  }
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
